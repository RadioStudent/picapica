// Generated by CoffeeScript 2.6.1
(function() {
  var TrackList, transformRequest;

  transformRequest = function(request) {
    request = angular.copy(request); // Get rid of references
    request.tracks = request.tracks.map(function(track) {
      if (!track.comment) {
        track.comment = '';
      }
      return track;
    });
    return angular.toJson(request);
  };

  TrackList = function($resource) {
    return $resource('api/v1/tracklists/:id', {}, {
      update: {
        method: 'PUT',
        transformRequest: transformRequest
      },
      save: {
        method: 'POST',
        transformRequest: transformRequest
      }
    });
  };

  module.exports = TrackList;

}).call(this);
